enum ButtonType { primary, secondary }

enum IconButtonType { popButton, playButton }

enum SocialButtonType { facebook, google, apple }

enum SocialMediaType { facebook, apple, google }

enum ContentType { video, text, button, document }

enum SecondaryIconButtonType { photoUploadButton, addButton, deleteButton }

